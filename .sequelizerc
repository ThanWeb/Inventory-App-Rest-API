const path = require("path")
const appDir = path.join(__dirname, 'app')
const configDir = path.join(__dirname, 'app/configs')
const dbDir = path.join(__dirname, 'app/db')

module.exports = {
  'config': path.join(configDir, 'database.json'),
  'models-path': path.join(appDir, 'models'),
  'migrations-path': path.join(dbDir, 'migrations'),
  'seeders-path': path.join(dbDir, 'seeds'),
}
